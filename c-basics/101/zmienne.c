#include <stdio.h>

int main() {
    int liczba = 10;       // Liczba całkowita
    float przecinek = 3.14; // Liczba zmiennoprzecinkowa
    char znak = 'A';       // Jeden znak
    printf("Liczba: %d, Float: %.2f, Znak: %c\n", liczba, przecinek, znak);
    return 0;
}
